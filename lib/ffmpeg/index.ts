/**
 * FFmpeg helpers (NOT IMPLEMENTED — STEP 1 placeholder).
 *
 * TODO: Build and run FFmpeg commands for the render worker:
 * - Concatenate scene clips/images into a 9:16 timeline.
 * - Mix narration (TTS) and background music with ducking.
 * - Burn in subtitles.
 * - Write outputs to data/renders and final files to data/exports.
 *
 * Runs on the server/worker side only.
 */
export {};
