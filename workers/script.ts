/**
 * Script worker (NOT IMPLEMENTED — STEP 1 placeholder).
 *
 * TODO: Turn validated research into a short-form script (hook, body,
 * CTA) following the user's Settings (tone, duration, hook/CTA style).
 * Validate with a Zod schema and save to Video.scriptJson.
 */
export {};
