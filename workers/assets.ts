/**
 * Assets worker (NOT IMPLEMENTED — STEP 1 placeholder).
 *
 * TODO: For each scene, search stock providers (lib/providers/stock),
 * download the chosen media to data/assets, create Asset rows and set
 * Scene.assetId to the selected asset.
 */
export {};
