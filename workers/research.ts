/**
 * Research worker (NOT IMPLEMENTED — STEP 1 placeholder).
 *
 * TODO: Gather source material and facts for the project topic, then
 * fact-check key claims. Store the validated research as the job result
 * for the SCRIPT step. Uses lib/ai/client with a Zod schema.
 */
export {};
